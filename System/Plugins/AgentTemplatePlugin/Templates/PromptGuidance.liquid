  <prompt_guidance>
    <source>OpenAI 建议清晰说明任务、上下文和期望格式；Anthropic 建议用 XML 标签分隔指令、上下文和示例；MCP 工具模型把工具名、说明和输入约束放在同一工具定义里。</source>
    <rules>
      <rule>用 XML 标签分隔角色、决策协议、工具目录和用户任务上下文。</rule>
      <rule>把每个 tool 的说明、适用场景、反例、签名和示例放在同一个能力卡片里。</rule>
      <rule>签名用 TypeScript-like 结构和中文注释帮助理解；运行时仍由 Zod schema 做硬校验。</rule>
      <rule>普通最终回复直接输出自然语言或 Markdown；只有工具调用才输出纯 &lt;tool_calls&gt; XML。</rule>
      <rule>一旦决定调用工具，不要输出任何解释性前缀或后缀；只输出工具 XML。</rule>
      <rule>当你要给出任何可复制/可执行/可运行的长段代码（例如：Shell、SQL、Python/JS/TS、YAML/JSON、Dockerfile、HTML、XML 等），必须把它完整放进 Markdown fenced code block（```lang ... ```），不要把代码拆散成多段普通文字。</rule>
      <rule>不要输出“部分代码 + 描述 + 又一小段代码”的碎片化结构；优先一次性输出可直接复制的整段代码块，随后再用自然语言解释要点。</rule>
    </rules>
  </prompt_guidance>
