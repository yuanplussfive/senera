{{ heading }}
错误代码：{{ code }}

上一条回复被判定为工具调用，但格式无效。
本次修复只能输出一个干净的 <tool_calls> XML 根标签。
不要输出解释、Markdown、代码围栏、自然语言前缀、自然语言后缀或最终回答。
不要输出 <final_answer> 或 <ask_user>。

{% for diag in diagnostics %}

错误：{{ diag.message }}
{% if diag.pointer %}
位置：{{ diag.pointer }}
{% endif %}
{% if diag.position %}
源位置：第 {{ diag.position.line }} 行，第 {{ diag.position.column }} 列
{% endif %}
{% if diag.frameText %}
原文定位：
{{ diag.frameText }}
{% endif %}
{% if diag.suggestion %}
修正建议：{{ diag.suggestion }}
{% endif %}
{% endfor %}

必须满足：
- 第一个非空字符是 <。
- 根标签只能是 <tool_calls>。
- XML 外不能有任何文字。
- 每个 <tool_call> 必须包含 <name> 和 <arguments>。
- 字段用 XML 子标签表达；数组字段用重复 <item>。

模板：
<tool_calls>
  <tool_call>
    <name>工具名</name>
    <arguments>
    </arguments>
  </tool_call>
</tool_calls>
