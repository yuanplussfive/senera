{
  "name": "AskUserTool",
  "observationProjection": "observations/default.projection.json",
  "description": "向用户提出一个简短澄清问题，等待用户补充关键信息。",
  "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "question": {
        "type": "string",
        "description": "必须。要展示给用户的澄清问题；应具体、简短。"
      },
      "reason_code": {
        "type": "string",
        "description": "可选。机器可读原因码，例如 missing_location、missing_scope。"
      }
    },
    "required": ["question"],
    "additionalProperties": false,
    "definitions": {}
  },
  "permissions": [],
  "execution": {
    "Targets": ["Local"],
    "Network": "Deny",
    "Workspace": "ReadOnly"
  },
  "runtime": {
    "Lifecycle": "Immediate",
    "ProtocolVersion": 2,
    "ResultAssessment": "ProcessExit"
  },
  "resources": [],
  "sources": [
    {
      "Id": "interaction",
      "Title": "Interaction",
      "Description": "Direct interaction with the user for clarification, confirmation, and decisions."
    }
  ],
  "search": {
    "Summary": "向用户提出一个简短澄清问题，等待用户补充关键信息。",
    "Tags": ["用户确认", "信息补充", "问题澄清", "选择决策"],
    "Capabilities": [
      {
        "Id": "conversation.ask-user",
        "Title": "Ask user",
        "Description": "Ask the user for missing essential information, confirmation, or disambiguation.",
        "Facets": {
          "Actions": ["ask", "clarify", "confirm"],
          "Targets": ["user", "conversation", "missing-information"],
          "Inputs": ["question", "reason-code"],
          "Outputs": ["clarification-request", "user-question"],
          "Evidence": ["clarification-request"],
          "Effects": ["blocks-run", "user-visible"]
        },
        "Aliases": ["问用户", "澄清问题", "ask for clarification", "confirm with user"],
        "Risk": {
          "SideEffect": "user-visible",
          "Permission": "none"
        }
      }
    ],
    "UseCases": ["缺少必要参数、目标不明确或继续执行有歧义时使用。"],
    "Examples": [
      "缺少搜索关键词确认",
      "删除目标二次确认",
      "ask user for missing location",
      "ambiguous required parameter"
    ],
    "Avoid": ["如果已经可以直接回答或直接调用工具，不要再询问用户。"]
  },
  "evidenceCapabilities": [],
  "artifacts": {
    "Evidence": [
      {
        "Kind": "clarification_request",
        "Records": "$.control",
        "Slots": {
          "kind": "$.kind",
          "question": "$.question",
          "reasonCode": "$.reason_code"
        },
        "Identity": {
          "Parts": [
            "question",
            {
              "Slot": "reasonCode",
              "Required": false
            }
          ]
        },
        "Presentation": {
          "Locator": "{% if reasonCode != blank %}{{ reasonCode }}{% else %}clarification{% endif %}",
          "Display": "clarification requested: {{ question }}",
          "Label": "{{ question }}",
          "Source": "{% if reasonCode != blank %}{{ reasonCode }}{% else %}missing_information{% endif %}"
        },
        "Confidence": 0.9,
        "Metadata": {
          "reasonCode": "$.reason_code"
        },
        "ModelProjection": {
          "Slots": ["question", "reasonCode"]
        },
        "PlannerMemory": {
          "Facts": ["question", "reasonCode"],
          "ArtifactRefs": ["evidence", "projection"]
        },
        "Projection": {
          "SummaryTemplate": "{% for e in evidence %}- evidenceUri: {{ e.evidenceUri }} ask user: {{ e.slots.question }}\n{% endfor %}",
          "ArtifactTemplate": "{% for e in evidence %}- evidenceUri: {{ e.evidenceUri }} clarification request\n  question: {{ e.slots.question }}\n  reason: {{ e.slots.reasonCode }}\n{% endfor %}"
        }
      }
    ],
    "Summary": {
      "Template": "{% for block in projections %}{{ block.summary }}\n{% endfor %}",
      "ArtifactTemplate": "{% for block in projections %}{{ block.artifact }}\n{% endfor %}{% if workspace %}{% for change in workspace.changes %}- {{ change.status }} {{ change.path }}\n{% endfor %}{% endif %}"
    }
  }
}
